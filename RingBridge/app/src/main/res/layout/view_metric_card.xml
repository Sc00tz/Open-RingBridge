<?xml version="1.0" encoding="utf-8"?>
<!--
  Reusable metric card for the Home dashboard grid.
  Included once per metric in fragment_home.xml with android:id overrides so each
  instance gets a unique root the fragment can look up. Child view IDs are shared
  but resolved relative to the included root via binding.<root>.findViewById.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="@color/card_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <!-- Label row: accent dot + metric name -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <View
                android:id="@+id/metricDot"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:layout_marginEnd="6dp"
                android:background="@drawable/dot_accent" />

            <TextView
                android:id="@+id/metricLabel"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Metric"
                android:textSize="12sp"
                android:textColor="@color/section_label_color" />
        </LinearLayout>

        <!-- Value + unit -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="bottom"
            android:layout_marginTop="6dp">

            <TextView
                android:id="@+id/metricValue"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="—"
                android:textSize="26sp"
                android:textStyle="bold"
                android:textColor="?android:attr/textColorPrimary" />

            <TextView
                android:id="@+id/metricUnit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:layout_marginBottom="3dp"
                android:text=""
                android:textSize="11sp"
                android:textColor="@color/section_label_color" />
        </LinearLayout>

        <!-- Sparkline preview (hidden when no data) -->
        <dev.ringbridge.SparklineView
            android:id="@+id/metricSparkline"
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:layout_marginTop="8dp" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
